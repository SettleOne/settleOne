{
  "name": "settleone",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:workspace": "turbo dev --filter=workspace",
    "dev:marketing": "turbo dev --filter=marketing",
    "dev:operations": "turbo dev --filter=operations",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "packageManager": "npm@10.8.0"
}
